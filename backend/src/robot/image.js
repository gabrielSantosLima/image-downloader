const google = require('googleapis').google
const searchTerm = google.customsearch('v1');
const credentials = require('../../credentials/imageDownload-8ad4a78e44b1.json')
const input = require('../input')

module.exports = {
    searchImages(query){
        
        return {};
    }
};